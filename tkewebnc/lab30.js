// Lab 30 — Mini Menu dùng Arrow Function
const calculator = {
 sum: (a, b) => a + b,
 subtract: (a, b) => a - b,
 multiply: (a, b) => a * b,
 divide: (a, b) => a / b
}
console.log(calculator.sum(3, 5))
console.log(calculator.multiply(4, 6))
